# All optional. With none of these set, Heat Sentinel runs fully:
#   - FortyGuard data  -> deterministic mock mode (labeled in the UI)
#   - Agent reasoning  -> deterministic rule-based mode (labeled in the UI)

# FortyGuard Temperature API (https://docs-api.fortyguard.com)
# FORTYGUARD_API_KEY=
# FORTYGUARD_API_BASE_URL=https://api.fortyguard.com

# Claude (agent reasoning layer)
# ANTHROPIC_API_KEY=
